@model EXPEDIT.Transactions.ViewModels.AccountViewModel
<h2>Partnership Status</h2>
<h4 style="color:black;">@if (AuthorizedFor(EXPEDIT.Transactions.Permissions.Partner)) { @Html.Raw("Partner</h4>") }
                              else {
                             @:Member Only</h4><p><a class="btn btn-small" href="/store/user/partneragreement"><i class="glyphicon icon-star"></i>&nbsp;<span>Become a Partner</span></a></p>
                             }
<h2>Contact Details</h2>
<p><a class="btn btn-small" href="/Users/Account/ChangePassword"><i class="glyphicon icon-lock"></i>&nbsp;<span>Change Password</span></a>&nbsp;<a class="btn btn-small" href="javascript:$('#ui-tabs-1').load('/store/user/UpdateProfilePartial');"><i class="glyphicon icon-user"></i>&nbsp;<span>Update Details</span></a></p>
<p><strong>@Model.Contact.Title @Model.Contact.Firstname @Model.Contact.Surname (@Model.Contact.Username)</strong></p>
<div class="editor-label">
    @Html.LabelFor(x => x.Contact.DefaultEmail)
</div>
<div class="editor-field">
    @Html.TextBoxFor(x => x.Contact.DefaultEmail, new { @readonly = true })
</div>
<div class="editor-label">
    @Html.LabelFor(x => x.Contact.DefaultMobile)
</div>
<div class="editor-field">
   @Html.TextBoxFor(x => x.Contact.DefaultMobile, new { @readonly = true })
</div>
<h3>Addresses</h3>
<table class="table table-striped">
<thead>
<tr><th></th></tr>
</thead>
<tbody>
    @foreach (var p in Model.Contact.ContactAddresses) {
<tr>   
<td>
    <p style="text-transform:uppercase">@p.AddressName</p>
    <p style="text-transform:uppercase">@p.Street @p.Extended @p.City @p.State @p.Country @p.Postcode</p>
    <p>@p.Email @p.Mobile @p.Phone</p>
</td>
</tr>
}
</tbody>
</table>